<ul class="subnav nav-pills nav-stacked nested_nav">
  <% @category.tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task), class: "nav-link #{}" %>
    </li>
  <% end %>
</ul>